# オプション: PyTorch GeometricによるGNNモジュール（train_gnn.py）用の追加依存関係。
# 通常のアプリ（app.py）には不要。requirements.txt / requirements-dev.txt の上に重ねて使う。
#
#   pip install -r requirements.txt -r requirements-gnn.txt
#
# 素の `pip install torch` は既定でCUDA対応wheelが入るため、GPU(CUDA)環境ではこのまま
# インストールするだけで train_gnn.py が --device auto（既定）でGPUを自動検出・優先使用します。
# 追加設定は不要です。
#
# CPU専用wheelでディスク使用量・ダウンロード量を抑えたい場合は以下を追加してください
# （ネットワークポリシーにより download.pytorch.org へ到達できない環境では通常のPyPIに
# フォールバックし、この場合もCUDA同梱wheelがインストールされますが動作に支障はありません）:
#   --extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2,<3
torch_geometric>=2.5,<3
